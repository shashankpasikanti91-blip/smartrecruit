"""
DEPLOYMENT & n8n INTEGRATION GUIDE
Production-ready setup for Recruitment AI System
"""

# 🚀 DEPLOYMENT GUIDE\n\n## Phase 1: Local Development Setup\n\n### 1.1 Prerequisites\n- Python 3.10+\n- Git\n- Node.js 18+ (for n8n)\n- Google Cloud Project with Drive API enabled\n- Supabase Account\n- OpenAI API Key\n\n### 1.2 Clone & Setup\n\n```bash\n# Navigate to project\ncd recruitment_ai_system\n\n# Create virtual environment\npython -m venv venv\nsource venv/bin/activate  # On Windows: venv\\Scripts\\activate\n\n# Install dependencies\npip install -r requirements.txt\n\n# Copy environment template\ncp .env.example .env\n```\n\n### 1.3 Configure Services\n\n**A. OpenAI Setup**\n```bash\n# Get API key from https://platform.openai.com/api-keys\n# Add to .env:\nOPENAI_API_KEY=sk-proj-...\nOPENAI_MODEL=gpt-4o-mini\n```\n\n**B. Supabase Setup**\n```bash\n# 1. Create project at https://supabase.com\n# 2. Copy credentials to .env:\nSUPABASE_URL=https://your-project.supabase.co\nSUPABASE_KEY=eyJhbGciOiJIUzI1NiIs...\n\n# 3. Run SQL to create tables (see SQL schema in README.md)\n```\n\n**C. Google Drive Setup**\n```bash\n# 1. Create service account:\n#    - Go to Google Cloud Console\n#    - Create new project\n#    - Enable Google Drive API\n#    - Create Service Account\n#    - Download JSON key\n\n# 2. Save to credentials/google_service_account.json\n\n# 3. Add to .env:\nGOOGLE_CREDENTIALS_PATH=./credentials/google_service_account.json\n\n# 4. Share Google Drive folders with service account email\n```\n\n**D. n8n Setup (Local)**\n```bash\n# Install n8n globally\nnpm install -g n8n\n\n# Run n8n\nn8n\n\n# Access at http://localhost:5678\n# Generate API key in Settings → API Keys\n\n# Add to .env:\nN8N_BASE_URL=http://localhost:5678\nN8N_API_KEY=your_api_key_here\n```\n\n### 1.4 Test Connection\n\n```bash\npython -c \"\nfrom recruitment_ai_system.utils import config\nconfig.validate()\nprint('✅ All configurations loaded successfully')\n\"\n```\n\n---\n\n## Phase 2: n8n Workflow Integration\n\n### 2.1 Export Existing Workflows\n\nYour JSON files contain valuable workflow definitions:\n\n```python\nfrom recruitment_ai_system.control_panel import ControlPanelManager\n\nmanager = ControlPanelManager()\n\n# Export as n8n format\nexported = manager.export_workflow_for_n8n()\n\n# Save to file\nimport json\nwith open(\"exported_workflow.json\", \"w\") as f:\n    json.dump(exported, f, indent=2)\n```\n\n### 2.2 Import into n8n\n\n1. Open n8n dashboard (http://localhost:5678)\n2. Click \"Import\" in top menu\n3. Select exported_workflow.json\n4. Update credentials (OpenAI, Google Drive, etc.)\n5. Activate workflow\n\n### 2.3 Connect Python Backend to n8n\n\n**Option A: Webhook Triggers (Recommended)**\n\n```python\n# In your n8n workflow, add Webhook node:\n# - Method: POST\n# - URL: /webhook/python-recruitment\n\n# In Python:\nfrom recruitment_ai_system.workflows import N8nClient\n\nclient = N8nClient()\n\n# Trigger webhook\nresponse = await client.trigger_workflow(\n    workflow_id=\"your_workflow_id\",\n    data={\n        \"candidate_id\": \"cand_123\",\n        \"resume_text\": \"...\",\n        \"action\": \"screen\"\n    }\n)\n```\n\n**Option B: Direct API Calls**\n\n```python\n# Configure specific workflow IDs\n.env:\nN8N_SCREENING_WORKFLOW_ID=workflow_abc123\nN8N_MESSAGING_WORKFLOW_ID=workflow_def456\nN8N_JD_PROCESSING_WORKFLOW_ID=workflow_ghi789\n\n# System automatically routes to correct workflows\n```\n\n### 2.4 Create Supporting Workflows\n\n**Workflow 1: Resume Screening**\n```\nWebhook (Resume Screening)\n  ↓\nSet Variables (extract skills, experience)\n  ↓\nNode: Call Python Backend\n  - POST to http://localhost:8000/api/screen\n  - Pass resume, JD, metadata\n  ↓\nUpdate Google Sheets (Results)\n  ↓\nSend Email (Confirmation)\n```\n\n**Workflow 2: Message Generation**\n```\nForm Trigger (Message request)\n  ↓\nNode: Call Python Backend\n  - Generate message via messaging_agent\n  ↓\nConditional: Check message type\n  ├─ If Email → Email node\n  ├─ If WhatsApp → Twilio node\n  └─ If LinkedIn → Manual task\n  ↓\nLog to Database\n```\n\n**Workflow 3: JD Processing**\n```\nGoogle Drive (New file)\n  ↓\nNode: Extract JD text\n  ↓\nNode: Call Python Backend\n  - Process JD, generate embeddings\n  ↓\nCreate Job Posts (multi-platform)\n  ↓\nPublish to channels\n```\n\n---\n\n## Phase 3: API Server Setup (Optional)\n\nFor REST API access to Python backend:\n\n```python\n# Create api_server.py\nfrom fastapi import FastAPI, HTTPException\nfrom recruitment_ai_system.main import RecruitmentAISystem\nimport uvicorn\nimport uuid\n\napp = FastAPI(title=\"Recruitment AI API\")\nsystem = RecruitmentAISystem()\n\n@app.post(\"/api/screen\")\nasync def screen_candidate(data: dict):\n    try:\n        result = await system.screen_candidate(\n            candidate_id=data.get(\"candidate_id\", str(uuid.uuid4())),\n            resume_text=data.get(\"resume_text\"),\n            jd_id=data.get(\"jd_id\"),\n            jd_text=data.get(\"jd_text\")\n        )\n        return {\"success\": True, \"data\": result}\n    except Exception as e:\n        raise HTTPException(status_code=400, detail=str(e))\n\n@app.post(\"/api/message\")\nasync def generate_message(data: dict):\n    try:\n        result = await system.generate_message(\n            message_type=data.get(\"message_type\"),\n            tone=data.get(\"tone\", \"professional\"),\n            platform=data.get(\"platform\"),\n            recipient_name=data.get(\"recipient_name\"),\n            recipient_email=data.get(\"recipient_email\"),\n            context=data.get(\"context\", {})\n        )\n        return {\"success\": True, \"data\": result}\n    except Exception as e:\n        raise HTTPException(status_code=400, detail=str(e))\n\n@app.get(\"/api/health\")\nasync def health_check():\n    return await system.health_check()\n\nif __name__ == \"__main__\":\n    uvicorn.run(app, host=\"0.0.0.0\", port=8000)\n```\n\nRun server:\n```bash\npip install fastapi uvicorn\npython api_server.py\n```\n\nn8n can now call:\n```\nPOST http://localhost:8000/api/screen\nPOST http://localhost:8000/api/message\nGET http://localhost:8000/api/health\n```\n\n---\n\n## Phase 4: Production Deployment\n\n### 4.1 Docker Setup\n\n```dockerfile\n# Dockerfile\nFROM python:3.10-slim\n\nWORKDIR /app\n\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\n\nCOPY recruitment_ai_system/ ./recruitment_ai_system/\nCOPY .env .\n\nEXPOSE 8000\n\nCMD [\"python\", \"api_server.py\"]\n```\n\nBuild and run:\n```bash\ndocker build -t recruitment-ai:latest .\ndocker run -p 8000:8000 --env-file .env recruitment-ai:latest\n```\n\n### 4.2 Cloud Deployment (AWS/GCP/Azure)\n\n**AWS EC2:**\n```bash\n# 1. Launch EC2 instance (Ubuntu 22.04)\n# 2. SSH into instance\n# 3. Install dependencies\nsudo apt update\nsudo apt install python3.10 python3-pip nodejs npm\n\n# 4. Clone repository\ngit clone <your-repo>\ncd recruitment_ai_system\n\n# 5. Setup and run\npip install -r requirements.txt\n# Configure .env with production values\npython api_server.py  # or use gunicorn\n```\n\n**Gunicorn Setup (Production):**\n```bash\npip install gunicorn\ngunicorn -w 4 -b 0.0.0.0:8000 api_server:app\n```\n\n### 4.3 Database Backups\n\n```sql\n-- Supabase automated backups enabled by default\n-- Manual backup:\npg_dump postgres://user:password@host/database > backup.sql\n\n-- Restore:\npsql postgres://user:password@host/database < backup.sql\n```\n\n### 4.4 Monitoring & Logging\n\n```python\n# Configure CloudWatch (AWS) / Stackdriver (GCP)\nimport logging\nimport watchtower\n\nlogger = logging.getLogger(__name__)\nlogger.addHandler(watchtower.CloudWatchLogHandler())\n```\n\n---\n\n## Phase 5: Linking with Your Local n8n Instance\n\n### 5.1 Direct Connection\n\nIf you want Python backend to directly call your local n8n:\n\n```bash\n# .env configuration\nN8N_BASE_URL=http://localhost:5678\nN8N_API_KEY=<your-n8n-api-key>\n\n# Get API key:\n# 1. n8n UI → Settings → API Keys\n# 2. Create new key\n# 3. Copy to .env\n```\n\n### 5.2 Test Connection\n\n```python\nfrom recruitment_ai_system.workflows import N8nClient\nimport asyncio\n\nasync def test():\n    client = N8nClient()\n    is_healthy = await client.health_check()\n    if is_healthy:\n        print(\"✅ Connected to n8n\")\n        workflows = await client.list_workflows()\n        print(f\"Found {len(workflows)} workflows\")\n    else:\n        print(\"❌ Failed to connect to n8n\")\n\nasyncio.run(test())\n```\n\n### 5.3 Configure Workflow IDs\n\nAfter creating workflows in n8n:\n\n```python\n# Each workflow has an ID visible in URL: /workflows/abc123\n# Add to .env:\nN8N_SCREENING_WORKFLOW_ID=abc123\nN8N_MESSAGING_WORKFLOW_ID=def456\nN8N_JD_PROCESSING_WORKFLOW_ID=ghi789\n```\n\n---\n\n## Troubleshooting Checklist\n\n### ✅ Pre-Flight Checklist\n- [ ] Python 3.10+ installed\n- [ ] All dependencies installed: `pip install -r requirements.txt`\n- [ ] .env file created with all required keys\n- [ ] Supabase tables created\n- [ ] Google credentials downloaded\n- [ ] OpenAI API key valid\n- [ ] n8n running locally (if using local instance)\n\n### ✅ Configuration Validation\n```bash\npython -c \"\nfrom recruitment_ai_system.utils import config\ntry:\n    config.validate()\n    print('✅ Configuration Valid')\nexcept ValueError as e:\n    print(f'❌ Configuration Error: {e}')\n\"\n```\n\n### ✅ Connectivity Tests\n```python\nimport asyncio\nfrom recruitment_ai_system.main import RecruitmentAISystem\n\nasync def test_all():\n    system = RecruitmentAISystem()\n    health = await system.health_check()\n    print(health)\n\nasyncio.run(test_all())\n```\n\n---\n\n## Performance Optimization\n\n### 1. Batch Processing\n```python\n# Screen multiple candidates at once\nresults = screening_agent.batch_screen(\n    candidates=[...],\n    jd_text=jd_text\n)\n```\n\n### 2. Caching Embeddings\n```python\n# Embeddings are expensive - cache frequently used ones\nembedding = embedding_engine.embed_text(text)\n# Store in Redis/memory for reuse\n```\n\n### 3. Async Operations\nAll I/O operations are async - use asyncio.gather() for parallelism:\n```python\nresults = await asyncio.gather(\n    system.screen_candidate(...),\n    system.screen_candidate(...),\n    system.screen_candidate(...)\n)\n```\n\n---\n\n## Security Best Practices\n\n1. **Environment Variables**: Never commit .env files\n2. **API Keys**: Rotate regularly\n3. **Database**: Enable Supabase RLS policies\n4. **Logs**: Don't log sensitive data\n5. **CORS**: Restrict to known domains\n6. **Rate Limiting**: Implement to prevent abuse\n\n---\n\n**Version:** 1.0.0\n**Last Updated:** February 2026\n