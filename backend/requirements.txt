# SRP SmartRecruit v3.2 — Production Requirements
# Generated: 2026-03-16
# Python: 3.11+ recommended (currently running 3.14)

# ── Core Framework ────────────────────────────────────────────
fastapi==0.129.0
uvicorn[standard]==0.40.0
gunicorn==22.0.0             # Production WSGI/ASGI server
python-multipart==0.0.22    # File upload support

# ── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.46
psycopg2-binary==2.9.11     # PostgreSQL driver
alembic==1.18.4              # DB migrations

# ── Validation & Settings ────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.1.0

# ── Authentication & Security ────────────────────────────────
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-dotenv==1.1.0

# ── AI / LLM ─────────────────────────────────────────────────
pydantic-ai==1.58.0
openai==2.20.0
anthropic==0.79.0

# ── Resume Processing ─────────────────────────────────────────
PyPDF2==3.0.1
python-docx==1.2.0
pdfplumber==0.11.9

# ── Email ────────────────────────────────────────────────────
aiosmtplib==5.1.0

# ── Utilities ────────────────────────────────────────────────
python-dateutil==2.8.2
httpx==0.27.0                # Async HTTP client (webhook dispatch, integrations)
pycryptodome==3.21.0         # AES-256 encryption for connector credentials

# ── Development / Testing (install separately in dev) ────────
# pytest==7.4.0
# pytest-asyncio==0.21.0
# httpx==0.27.0

jinja2==3.1.3
